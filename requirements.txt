# ---- Web server ----
flask==3.0.3

# ---- Env / config ----
python-dotenv==1.0.1

# ---- PDF loading ----
PyMuPDF==1.24.9

# ---- Chunking ----
langchain-text-splitters==0.2.4

# ---- Embeddings + reranker ----
# NOTE: torch must be >=2.6 — newer `transformers` refuses to load
# non-safetensors model checkpoints (like some biomedical models) on
# older torch versions for security reasons (CVE-2025-32434).
sentence-transformers==3.3.1
torch==2.6.0

# ---- Vector DB (hybrid: dense + sparse) ----
pinecone==5.0.1
pinecone-text==0.9.0

# ---- LLM ----
# NOTE: openai's internal client still passes a `proxies` argument that
# httpx removed in 0.28 — pinning httpx below that avoids the crash:
#   TypeError: Client.__init__() got an unexpected keyword argument 'proxies'
openai==1.54.4
httpx==0.27.2
